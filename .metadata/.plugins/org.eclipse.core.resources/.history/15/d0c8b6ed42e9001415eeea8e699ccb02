import java.awt.*;
import java.util.ArrayList;

import javax.swing.*;

public class main {

	public static void main(String[] args) {

		//JFrame calendario = new calendarioFrame();
		//calendario.setVisible(true);
		
		Gestor g = new Gestor();
		//CreadorProyectos p = new CreadorProyectos(g);
		//CreadorTareas t = new CreadorTareas(g);		
		
		tarea t = new tarea(1,"tarea0", new fecha(), new fecha(), new hora(), new hora(), "desc",0,new contexto("en el baño"));
		g.agregarProyecto(new proyecto(0));
		ArrayList<tarea> TAREAS = new ArrayList<tarea>();
		
		
		//g.getProyecto(0).setTareas();
		
		Resumen r = new Resumen(g);
		
		
		
		
	}
}
